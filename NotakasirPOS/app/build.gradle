plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
    id("kotlin-kapt")
    id("androidx.navigation.safeargs.kotlin")
}

android {
    namespace = "id.notakasir.pos"
    compileSdk = 34

    defaultConfig {
        applicationId = "id.notakasir.pos"
        minSdk = 24
        targetSdk = 34
        versionCode = 1
        versionName = "1.0.0"

        testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"
        vectorDrawables { useSupportLibrary = true }
    }

    buildTypes {
        debug {
            applicationIdSuffix = ".debug"
            versionNameSuffix = "-debug"
        }
        release {
            minifyEnabled = true
            shrinkResources = true
            proguardFiles(
                getDefaultProguardFile("proguard-android-optimize.txt"),
                "proguard-rules.pro"
            )
            // === SIGNING RILIS (isi di JStudio) ===
            // Cara 1 (disarankan di JStudio): Build > Generate Signed Bundle/APK,
            // JStudio akan mengisi signingConfig otomatis.
            // Cara 2 manual: buat file keystore.properties di root project:
            //   storeFile=/sdcard/notakasir.keystore
            //   storePassword=xxxx
            //   keyAlias=notakasir
            //   keyPassword=xxxx
            // lalu uncomment blok signingConfigs di bawah.
            //
            // signingConfigs {
            //     create("release") {
            //         val props = java.util.Properties()
            //         val f = rootProject.file("keystore.properties")
            //         if (f.exists()) {
            //             props.load(f.inputStream())
            //             storeFile = file(props["storeFile"] as String)
            //             storePassword = props["storePassword"] as String
            //             keyAlias = props["keyAlias"] as String
            //             keyPassword = props["keyPassword"] as String
            //         }
            //     }
            // }
            // buildTypes.release.signingConfig = signingConfigs.getByName("release")
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    kotlinOptions { jvmTarget = "17" }

    buildFeatures {
        viewBinding = true
        buildConfig = true
    }
}

dependencies {
    // --- AndroidX inti (ringan, wajib) ---
    implementation("androidx.core:core-splashscreen:1.0.1")
    implementation("androidx.core:core-ktx:1.12.0")
    implementation("androidx.appcompat:appcompat:1.6.1")
    implementation("com.google.android.material:material:1.12.0")
    implementation("androidx.constraintlayout:constraintlayout:2.1.4")
    implementation("androidx.recyclerview:recyclerview:1.3.2")
    implementation("androidx.swiperefreshlayout:swiperefreshlayout:1.1.0")

    // --- Lifecycle / ViewModel (MVVM) ---
    implementation("androidx.lifecycle:lifecycle-viewmodel-ktx:2.7.0")
    implementation("androidx.lifecycle:lifecycle-livedata-ktx:2.7.0")
    implementation("androidx.lifecycle:lifecycle-runtime-ktx:2.7.0")
    implementation("androidx.activity:activity-ktx:1.8.2")
    implementation("androidx.fragment:fragment-ktx:1.6.2")

    // --- Navigation Component (single-activity) ---
    implementation("androidx.navigation:navigation-fragment-ktx:2.7.7")
    implementation("androidx.navigation:navigation-ui-ktx:2.7.7")

    // --- Room (SQLite lokal) ---
    implementation("androidx.room:room-runtime:2.6.1")
    implementation("androidx.room:room-ktx:2.6.1")
    kapt("androidx.room:room-compiler:2.6.1")

    // --- Scan barcode (ZXing, lebih ringan dari MLKit+CameraX di HP) ---
    implementation("com.journeyapps:zxing-android-embedded:4.3.0")

    // --- Chart analitik ---
    implementation("com.github.PhilJay:MPAndroidChart:v3.1.0")

    // Printer thermal: TANPA library tambahan — pakai BluetoothSocket + EscPosPrinter buatan sendiri
    // (lihat util/printer). Nol dependensi ekstra = aman di Gradle HP.
}
